syntax = "proto3";

package item;

service Item {
  rpc getDataDB (Value) returns (stream Respuesta) {}
  rpc getDataCache (Value) returns (stream Respuesta) {}
}

message Value {
  string value = 1;
}

message Respuesta{
  string items = 1;
}